/** All Phaser asset keys in one place — never use raw strings in scene code. */
export const enum TextureKey {
  // Example: Logo = 'logo',
}

export const enum AudioKey {
  // Example: BGM = 'bgm',
}

export const enum SceneKey {
  Boot = 'BootScene',
  Preload = 'PreloadScene',
  MainMenu = 'MainMenuScene',
  Game = 'GameScene',
}
